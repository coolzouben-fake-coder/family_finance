<view class="weui-cell metric">
  <view class="weui-cell__bd">
    <view class="label">{{label}}</view>
    <view class="value">{{value}}</view>
    <view wx:if="{{hint}}" class="hint">{{hint}}</view>
  </view>
</view>
