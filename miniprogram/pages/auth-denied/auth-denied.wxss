.denied {
  min-height: 100vh;
  padding-top: 30vh;
}

.title {
  font-size: var(--font-title);
  font-weight: 700;
}

.desc {
  margin-top: 16rpx;
  color: var(--color-secondary-text);
  font-size: var(--font-body);
}
